package mybatis.log.query;

import com.mybatis.log.Mapper4Log;

public class MapperQuery implements IQuery {
    private Mapper4Log mapper;

    @Override
    public Object query(long id) {
        return query(id);
    }
}
